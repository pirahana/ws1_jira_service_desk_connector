const { describe, it } = require('mocha')
const assert = require('assert')

describe('jira_rest tests:', function () {
  it('', function () {
    var goo = 1
    assert(goo === 1)
    console.log('fii;')
  })
})
